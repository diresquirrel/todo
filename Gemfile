source 'https://rubygems.org'

gem 'mysql2'
gem 'dotenv-rails', :groups => [:development, :test]

# To update run: git submodule update --init --recursive
gem 'essential', path: './essential'
eval(IO.read('./essential/Gemfile'), binding)
